apiVersion: v1
kind: Service
metadata:
  name: {{ include "full-webapp.fullname" . }}
  labels:
    {{- include "full-webapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "full-webapp.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
