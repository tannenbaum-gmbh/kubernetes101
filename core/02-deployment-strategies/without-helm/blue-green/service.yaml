# Service selector is switched between blue and green to perform cutover
apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  type: ClusterIP
  selector:
    app: nginx
    version: blue   # Change to "green" to switch traffic
  ports:
    - port: 80
      targetPort: 80
