replicaCount: 2

image:
  repository: nginx
  tag: "1.25"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP   # ClusterIP | NodePort | LoadBalancer
  port: 80
  nodePort: ""      # Only used when type is NodePort (e.g. "30080")
